Official artwork. Rules in `BRAND.md`.

Ship `vytra_mark.png` and `vytra_logo_lockup.png`.  
Launcher: `vytra_app_icon_light.png`. Dark splash: `vytra_app_icon_dark.png`.

Do not redraw the V, the leaf-pulse, or the wordmark.  
Do not print the designer note “vitality + diagnostics”.
